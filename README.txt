The file to run to evaluate the models is run-models.py / run-models.ipynb (they are the same). The requirements
to run THIS file are in the requierements.txt. Other files, like detailing how to train the models are there, 
but they are not guranteed to have all the dependencies installed.